{% extends "blog/base.html" %}
{% load crispy_forms_tags %}

{% block content %}
<div class="container mx-auto px-4 py-8 max-w-3xl">
    <div class="bg-white p-8 rounded-xl shadow-2xl">
        <!-- Dynamic Form Title: Changes based on whether 'object' (the post) exists -->
        <h1 class="text-3xl font-bold text-gray-900 mb-6 border-b pb-2">
            {% if object %}
                Update Post: "{{ object.title }}"
            {% else %}
                Create New Post
            {% endif %}
        </h1>

        <form method="POST">
            {% csrf_token %}
            
            <!-- Render the form fields using crispy-forms filter -->
            <fieldset class="form-group mb-6">
                {{ form|crispy }}
            </fieldset>
            
            <div class="form-group">
                <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-4 rounded-lg focus:outline-none focus:shadow-outline transition duration-150 ease-in-out shadow-md hover:shadow-lg">
                    {% if object %}
                        Update Post
                    {% else %}
                        Publish Post
                    {% endif %}
                </button>
            </div>
        </form>
    </div>
</div>
{% endblock content %}